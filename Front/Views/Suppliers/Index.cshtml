@model IEnumerable<Front.Models.Supplier>

@{
    ViewBag.Title = "Fornecedores";
}

<h2>Fornecedores</h2>

<p>
    @Html.ActionLink("Inserir novo fornecedor", "Create", "List", new { @class = "btn btn-primary" })
</p>
<table class="table">
    <tr>
        <th>
            Nome
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.SupplierId }) |
            @Html.ActionLink("Detalhes", "Details", new { id=item.SupplierId }) |
            @Html.ActionLink("Excluir", "Delete", new { id=item.SupplierId })
        </td>
    </tr>
}

</table>
